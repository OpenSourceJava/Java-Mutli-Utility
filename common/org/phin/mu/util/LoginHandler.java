package org.phin.mu.util;

import org.phin.mu.lib.Strings;

public class LoginHandler {
	
	public static void login(String password, String username) {
		if ((password.equals(Strings.ADMIN_PASSWORD)) && (username.equals(Strings.ADMIN_USERNAME))) {
			// create admin frame
		} // TODO
	}

}
